# Hoist Aurelia sub-packages so the @aurelia/vite-plugin generated code
# (which imports @aurelia/runtime-html, @aurelia/kernel, etc. directly)
# can resolve them from any workspace package.
public-hoist-pattern[]=@aurelia/*
