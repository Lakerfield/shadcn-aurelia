---
'shadcn-aurelia': patch
---

New `create` command: scaffolds a new Aurelia 2 + Vite 7 + Tailwind v4 app from the template bundled with the CLI, with the primitives dependency pinned to a real version (replaces the `npx degit` instruction, which shipped an npm-incompatible `workspace:*` dependency).
